# Manasikuna v1.7.1 — Widget Test Scroll Fix

## السبب
فشل اختبار:

`manasikuna development mode app renders mobile core services`

لأن الاختبار كان يبحث عن النص `خدمات الحج والعمرة` قبل التمرير، بينما هذا العنوان موجود داخل `ListView` أسفل الجزء الظاهر من شاشة الاختبار، ولم يكن مبنيًا بعد بسبب البناء الكسول لعناصر القائمة.

## التعديل
تم تعديل الملف:

`munasakna_mobile/test/widget_test.dart`

بحيث يتم:

1. إبقاء فحص العناصر الظاهرة مباشرة كما هو.
2. التمرير إلى قسم الخدمات قبل فحص `خدمات الحج والعمرة`.
3. التمرير إلى خدمة `دليل المناسك` قبل فحصها.
4. عدم تعديل واجهة التطبيق أو بنية الصفحة.

## خارج النطاق
- لم يتم تغيير الصفحة الرئيسية.
- لم يتم تغيير التصميم.
- لم يتم تغيير بنية التطبيق.
- لم يتم إضافة تسجيل دخول.
- لم يتم ربط قاعدة بيانات.

## أمر التحقق

```bash
flutter clean
flutter pub get
flutter test
```
