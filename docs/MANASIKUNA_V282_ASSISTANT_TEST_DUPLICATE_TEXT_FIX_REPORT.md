# MANASIKUNA v2.8.2 — Assistant Test Duplicate Text Fix

## النطاق
إصلاح موضعي لاختبار صفحة المساعد الصوتي بعد ظهور عبارة "مساعد مناسكنا الإرشادي" في أكثر من موضع داخل الواجهة.

## سبب الخطأ
كان الاختبار يستخدم `findsOneWidget` مع `find.textContaining('مساعد مناسكنا الإرشادي')`، بينما تظهر العبارة في عنوان المساعد وفي النص التعريفي، لذلك فشل الاختبار بسبب وجود أكثر من نتيجة.

## الإصلاح
- تعديل `test/widget_test.dart`.
- استبدال التوقع إلى `findsAtLeastNWidgets(1)` لهذا النص فقط.
- لم يتم تعديل سلوك التطبيق أو الواجهات.

## النسخة
`2.8.2+27`
